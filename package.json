{
  "name": "workspace",
  "private": true,
  "version": "0.1.0",
  "description": "Adaptive desktop workspace — local-first intelligent desktop layer",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter @workspace/app dev",
    "build": "pnpm --filter @workspace/app build",
    "tauri:build": "pnpm --filter @workspace/app exec tauri build",
    "test": "pnpm --filter @workspace/tests test && node scripts/verify-explanation-catalog.mjs && node scripts/verify-ui-experience-boundary.mjs && node scripts/verify-content-security-policy.mjs",
    "typecheck": "pnpm --filter @workspace/app typecheck",
    "verify:explanation-catalog": "node scripts/verify-explanation-catalog.mjs",
    "verify:ui-experience-boundary": "node scripts/verify-ui-experience-boundary.mjs",
    "verify:content-security-policy": "node scripts/verify-content-security-policy.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.0"
}
