/**
 * Attention facade — implemented by the cognitive layer (Sprint 45).
 * Prefer `useCognitiveEngine` for new cognitive behaviour.
 */
export {
  useAttentionEngine,
  useAttentionRegistration,
} from "./CognitiveEngine";
