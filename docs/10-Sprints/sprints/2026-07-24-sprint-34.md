# Sprint 34 — Layout Save / Restore
(Existing IPC · Session Persistence)

| Field | Value |
|-------|-------|
| **Date** | 2026-07-24 |
| **Phase** | Phase 1 — Core Platform |
| **Status** | Complete |
| **Dependencies** | Sprint 13 layout stack, Sprint 33 canvas shell |

---

## Objective

Close the product loop: arrange zones → persist → reopen → same positions.

Also close the stale roadmap item **SQLite schema v1 (layouts, preferences)** by verification — schema already exists (`007_layout.sql`, settings key/value).

---

## Architecture Decision

**Option A: Frontend bootstrap + update_layout**

```
ensureLayout = get_layout | create_layout on layout_not_found
drag-end / pan-end / zoom debounce → update_layout(viewport, nodes)
```

No schema redesign. No auto-create layout inside `CreateWorkspace`.

---

## Persistence Decision

**Reuse Sprint 13.** Active workspace id remains in `localStorage` (`workspace.active_id`).

---

## Governance Decision

**No new capability.** Existing layout IPC.

---

## Audit Decision

**No new event types.**

---

## Files Created / Modified

| File | Purpose |
|------|---------|
| `app/src/lib/layoutPersistence.ts` | ensure / merge / save helpers |
| `app/src/components/CanvasShell.tsx` | Persist on drag/pan/zoom |
| Docs + ROADMAP | Mark schema v1 + save/restore done |

---

## Verification

```bash
pnpm typecheck
pnpm build
cargo check --workspace
```

Manual: create workspace → seed zones → Canvas → drag → restart app → positions restored.

---

## Remaining Risks

| Risk | Mitigation |
|------|------------|
| Nodes must exist in graph | Only zone ResourceRefs from create_zone |
| Multi-layout per workspace | Out of scope (one layout per workspace) |
