# Sprint 36 — Desktop Window Observation Surface
(Kernel + IPC + Operator)

| Field | Value |
|-------|-------|
| **Date** | 2026-07-24 |
| **Phase** | Phase 1 — Core Platform |
| **Status** | Complete |
| **Dependencies** | Sprint 35 |

---

## Objective

Expose Sprint 35 enumeration through governed kernel read → Tauri IPC → Operator console list.

---

## Architecture Decision

```
platform_window_enumerator()
  ↓
DesktopWindowService::list_recent
  ↓
GetDesktopWindows (governed, application_read, System)
  ↓
get_desktop_windows IPC → Operator "Desktop windows"
```

Observation only — no focus steal, no move/resize, no launch.

---

## Persistence Decision

**None.**

---

## Governance Decision

`GetDesktopWindows` — Governed, `application_read`, System. No new capability.

---

## Audit Decision

Governed query uses existing pipeline audit.

---

## Files Created / Modified

| File | Purpose |
|------|---------|
| `packages/kernel/.../desktop_window.rs` | Service |
| `packages/kernel/.../get_desktop_windows.rs` | Command |
| `app/src-tauri/.../desktop_window.rs` | IPC |
| Operator console + domain types | UI |
| Docs + ROADMAP | Record |

---

## Verification

```bash
cargo test -p workspace-kernel desktop_window
cargo check --workspace
pnpm typecheck
```
