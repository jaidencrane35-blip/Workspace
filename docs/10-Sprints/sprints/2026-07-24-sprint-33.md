# Sprint 33 — Spatial Workspace Canvas Shell Prototype
(DEC-009 · Front-End Presentation)

| Field | Value |
|-------|-------|
| **Date** | 2026-07-24 |
| **Phase** | Phase 1 — Core Platform |
| **Status** | Complete |
| **Dependencies** | Sprint 01–32 (layout IPC from Sprint 13) |

---

## Objective

Ship the first Spatial Workspace Canvas shell so zones render as draggable rectangles on a pan/zoom surface (DEC-009), with fixed chrome (Canvas / Operator tabs).

Positions may be in-memory until Sprint 34 wires persistence — this batch includes both.

---

## Architecture Decision

**Option A: React canvas over existing layout IPC**

Graph = what exists · Layout = where it is · Canvas = how it's shown.

No new kernel commands, capabilities, or migrations.

---

## Persistence Decision

Deferred to Sprint 34 in the same batch (drag-end / viewport → `update_layout`).

---

## Governance Decision

**No new capability.** Layout writes use existing `layout.write` path via IPC.

---

## Audit Decision

**No new events** beyond existing layout command audit.

---

## Files Created / Modified

| File | Purpose |
|------|---------|
| `app/src/components/CanvasShell.tsx` | Pan/zoom canvas + zone drag |
| `app/src/components/OperatorConsole.tsx` | Former App operator UI |
| `app/src/types/layout.ts` | Layout DTO mirrors |
| `app/src/App.tsx` / `App.css` | Fixed chrome + tabs |
| Sprint / architecture docs | Record |

---

## Verification

```bash
pnpm typecheck
pnpm build
cargo check --workspace
```

---

## Remaining Risks

| Risk | Mitigation |
|------|------------|
| Prototype only — not production design | Explicit DEC-009 Phase 1 shell |
| Apps/widgets/suggestions not on canvas yet | Future sprints |
