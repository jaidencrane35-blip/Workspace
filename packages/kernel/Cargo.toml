[package]
name = "workspace-kernel"
description = "Workspace core runtime boundary (Platform Kernel — future)"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[lib]
name = "workspace_kernel"
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
log = { workspace = true }
workspace-database = { workspace = true }

[dev-dependencies]
tempfile = "3"
